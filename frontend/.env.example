# Frontend environment variables
# For production deployments (e.g. Vercel), set this to your backend URL.
# Example: VITE_API_URL=https://your-backend.onrender.com
VITE_API_URL=http://localhost:5000

# Used only by Vite dev proxy target (optional)
BACKEND_PORT=5000
